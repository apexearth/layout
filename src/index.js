/**
 * ## Install
 * ```
 * npm install @apexearth/layout
 * ```
 *
 * ## Usage
 * ```javascript
 * const Layout = require('@apexearth/layout')
 * ```
 *
 * @module @apexearth/layout
 */
module.exports = require('./Layout')